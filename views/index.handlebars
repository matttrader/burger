<h1>Burger</h1>

<h2>These are the BURGERS to CHEW!</h2>

<ul>
{{#each burgerData}}
    {{#if burger_name}}
          {{> Burgers/burger_name eat=true}}
<li>{{this.burger_name}}</li>
{{/each}}
</ul>

<h2>These are the BURGERS to SPEW!</h2>
<ul>
  {{#each burgerData}}
    {{#if burger_name}}
        {{> Burgers/burger_name eat=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <div class="form-group">
    <label for= ??
    <input type= ??
    <input type= ??
  </div>

  <button type="submit">Please add your favorite burger.</button>
</form>